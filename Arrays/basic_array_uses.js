// Exercise 1
 
function firstElementOF(arr) {
  return arr[0];
}

// Exercise 2

function lastElementOf(arr) {
  return arr[arr.length - 1];
}

// Exercise 3

function nthElementOf(arr, index) {
  return arr[index];
}

// Exercise 4
// If a negative number is used to insert data on an array object, that data is assigned as a property of the object.

// Exercise 5

function firstNOf(arr, count) {
  return arr.slice(0, count);
}

// Exercise 6

function lastNOf(arr, count) {
  return arr.slice(-count);
}

// Exercise 7


// Exercise 8

function endsOf(firstArray, lastArray) {
  return [firstArray[0], lastArray[lastArray.length - 1]];
}

